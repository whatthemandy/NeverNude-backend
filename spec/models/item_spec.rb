require_relative '../../app/models/item.rb'

RSpec.describe Item do
  # it { expect(subject).to validate_presence_of(:user_id) }
  # it { expect(subject).to validate_presence_of(:section_id).only_integer }

  # it { expect(subject).to have_many(:outfits_items) }
  # it { expect(subject).to have_many(:outfits) }
  # it { expect(subject).to have_many(:tags_items) }
  # it { expect(subject).to have_many(:tags) }

  # it { should have_attached_file(:image) }
  # it { should validate_attachment_presence(:image) }


  # it { should validate_attachment_content_type(:icon).
  #     allowing('image/png', 'image/gif').
  #     rejecting('text/plain', 'text/xml') }


  # it { should validate_attachment_size(:image).
  #               less_than(2.megabytes) }
  # it { should validate_attachment_size(:icon).
                # greater_than(1024) }
  # it { should validate_attachment_size(:icon).
                # in(0..100) }
end
